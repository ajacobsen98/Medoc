import { resolvers } from '/.../src/resolvers';

describe('Resolvers', () => {
  describe('Query', () => {
    test('products', async () => {
      // Write test cases for the products query
      
    });

    // Add tests for other queries
  });

  describe('Mutation', () => {
    test('createUser', async () => {
      // Write test cases for the createUser mutation
    });

    // Add tests for other mutations
  });
});
